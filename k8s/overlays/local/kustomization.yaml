apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  - ../../api
  - ../../monitoring
namespace: opsight
images:
  - name: opsight/api
    newTag: local
  - name: opsight/payment-gateway
    newTag: local
  - name: opsight/ai-rca
    newTag: local
patchesJson6902:
  - target:
      group: rbac.authorization.k8s.io
      version: v1
      kind: ClusterRole
      name: kube-state-metrics
    patch: |-
      - op: replace
        path: /metadata/name
        value: kube-state-metrics-opsight
  - target:
      group: rbac.authorization.k8s.io
      version: v1
      kind: ClusterRoleBinding
      name: kube-state-metrics
    patch: |-
      - op: replace
        path: /metadata/name
        value: kube-state-metrics-opsight
      - op: replace
        path: /roleRef/name
        value: kube-state-metrics-opsight
patchesStrategicMerge:
  - environment-config.yaml
  - ingress.yaml
  - replicas.yaml
  - resources.yaml
